---
description: Initialize project context by analyzing the entire codebase, frameworks, database, dependencies, and generating comprehensive documentation as the AI agent's primary reference.
---

Execute the GAO Agent **`/context-init`** workflow.

**Authoritative source:** [`.agent/workflows/context-init.md`](../../.agent/workflows/context-init.md)

Read that file in full and execute every phase exactly as specified, applying the cross-platform translations documented in [`CLAUDE.md`](../../CLAUDE.md) (e.g. `// turbo` = auto-proceed without confirmation; Antigravity tool names → Claude equivalents; skills accessed by path under `.agent/skills/`).

**User input:** $ARGUMENTS
