---
description: Deploy application to target environment. Auto-detects framework and routes to the correct deployment skill with pre-deployment checks.
argument-hint: [target environment]
---

Execute the GAO Agent **`/context-deploy`** workflow.

**Authoritative source:** [`.agent/workflows/context-deploy.md`](../../.agent/workflows/context-deploy.md)

Read that file in full and execute every phase exactly as specified, applying the cross-platform translations documented in [`CLAUDE.md`](../../CLAUDE.md).

**User input:** $ARGUMENTS
