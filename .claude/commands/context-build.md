---
description: Context-aware build — automatically detects the project framework/language and executes the correct build command with validation and error handling.
argument-hint: [optional target or framework hint]
---

Execute the GAO Agent **`/context-build`** workflow.

**Authoritative source:** [`.agent/workflows/context-build.md`](../../.agent/workflows/context-build.md)

Read that file in full and execute every phase exactly as specified, applying the cross-platform translations documented in [`CLAUDE.md`](../../CLAUDE.md).

**User input:** $ARGUMENTS
